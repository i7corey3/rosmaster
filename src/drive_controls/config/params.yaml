maunal_control:
  ros__parameters:
    use_sim_time: false
    mqtt_ip: "localhost"
    mqtt_listener: "manual"
    mqtt_topic: "DriveControl"
    x_axis_max: 4.0
    x_axis_min: -4.0
    z_axis_max: 12.0
    z_axis_min: -12.0
